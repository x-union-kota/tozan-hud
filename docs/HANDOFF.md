# 登山HUD v3.2 引き継ぎ書(Claude Code 向け)

作成: 2026-08-30 / dist build: 0830-1350J / コア156+スモーク149+ツール94件 全通過
前版(v3.1引き継ぎ書)からの差分: 残タスク1「N8 目標ペース(等速仮想走者)」を実装し、ゴーストソース4つが揃った。運用指示は リポジトリ直下の `CLAUDE.md` に集約。

---

## 0. 再開手順

このフォルダ一式がそのまま作業リポジトリ。展開したら:

```bash
npm install jsdom
python3 tools/build.py
node test/core.test.js && node test/app.smoke.js && python3 test/gpx2route.test.py
```

3スイート全通過(156+149+94)を確認してから作業開始。

前提知識は3文書:
- `CLAUDE.md` — 守るべき制約・コマンド・設計思想(毎回読む)
- この文書 — 何が実装済みで何が残っているか
- `tozan-hud-experience-design.md`(体験設計書・**このzipには未同梱**。Kotaが別途渡す) — なぜこう動くべきか

---

## 1. 現在地: v3.2 実装状況サマリー

### ✓ 実装済み・機能する

- **N7 透視エンジン**: 汎用方位レジストリ、POI(温泉/駅/小屋)透視、自宅透視、太陽軌道、日没マーカー、可視(実線)/透視(破線)の視覚区別
- **N7b 星座同定**: 空レイヤ(↑↓で地上⇄空切替)、惑星・月、夜間暗赤パレット、`?t=HH:MM`時刻上書き
- **N8 背中**: 前回の自分/1周目の自分/標準CT/目標ペースの4ソース、15秒間隔トラック記録、正直さゲート(鮮度90秒・ルート外150m・未収束で消灯)、追い抜きヒステリシス20m
- **[v3.2] N8 目標ペース(等速仮想走者)**: ゴーストソース4つ目が揃った。ready画面の `↑`(従来は`↓`と同じ診断トグルで1つ余っていた)を目標ペース層に割り当て、`←→`で5分きざみ、`Enter`で確定、`Escape`で編集を捨てて1つ戻る。
  範囲は標準CTの0.4〜1.6倍、下限より下は「設定しない」。ルート単位で `thud.paceGoal.<routeId>` に永続。
  明示設定なので他ソースより優先し、2周目の「1周目の自分」にも奪われない。時計だけで決まるので復元(resume)でもそのまま効く。
  **周回ルートでは目標は「1周のタイム」**で、ラップごとに仮想走者が走り直す(`lapStartMs` 基準)。断面パネルとサマリーの差分表示も目標ペース基準に切替済み
- **[v3.2] 地形レイヤ = 数値DEMの等高線(DATA_SOURCES 優先1のランタイム側)**: `hillshademap` の z13固定ラスタ反転を廃止し、
  地理院 標高タイル(`dem5a_png` z15 / `dem_png` 〜z14)をデコードして等高線を自前描画。
  タイル枚数が12枚に収まる最大ズームを自動選択、dem5a の欠損域が半数を超えたら dem_png(z14)へ自動フォールバック。
  等高線間隔は**画面上の線間隔**から決める(レンジだけだと急斜面で数px間隔に潰れて白い塊になる。勾配60%点で10px以上空くまで間隔を上げる)。
  地図は非活性色1色に統一し主曲線は太さで区別(明るい色はルートと現在地に取っておく)。スケールバー/N↑/クレジットの下は `clearRect` で消す。
  実タイルで確認済み: 高尾山=50m間隔、皇居ラン=5m間隔(旧実装ではほぼ無地だった堀と台地が出る)、富士山=100m間隔
- **[v3.2] gpx2route.py のDEM投入口(DATA_SOURCES 優先1のツール側)**: `--emit-dem-fetch` が標高タイルのDLスクリプトを出力し、
  手元で実行して `--dem-tiles dem/` で投入する(Overpassと同じオフライン2段構え)。PNGデコードは **stdlibのzlibだけ**で書いてある(Pillow不要)。
  投入すると (a) 峰の可視判定をレイキャストで自動化して `v:1` を立てる、(b) 標高欠損POIを埋める、(c) GPXのele信頼性を検査する。
  **レイキャストの設計上の勘所** — 低ズームDEMは尾根を平滑化して標高を過小評価し(z11実測: 高尾山599→591m、富士山3776→3761m)、
  これは「見える」側に偏るので危険。地形側は周囲1セルの最大(`elev_max`)で見る。
  視線に一律の余裕を持たせる方式は**不可**(観測点の足元では視線が必ず地面すれすれを通るので、実際に見える対象まで落ちる。実測で確認)。
  代わりに両端 `max(60m, 2セル)` 以内のサンプルを除外する。地球の丸み+大気差は等価半径7/6で補正。
  DEMが欠けた区間を含む視線は「不明」であって「見える」ではない → `v` は 0 のまま。
  実タイル(z11)で確認済み: 高尾山から 富士山・丹沢山・大山・大岳山・御岳山=可視 / 雲取山・箱根山=遮蔽
- **[v3.2] 街中の地図 = 焼き込みOSM道路ベクタ(DATA_SOURCES 優先2)**: `route.vec` があり domain が urban なら、
  等高線を取りに行かず道路/鉄道/水域を自前描画する。等高線と同じく非活性色1色にして、道路クラスは太さ・鉄道と水域は線種で区別。
  クレジットは「地図: © OpenStreetMap contributors」。canvasが使えない環境では従来どおり線図へ縮退する。
  実データ確認済み: 皇居ラン(実Overpass応答・880本・約24KB)で丸の内の街路と堀が読める
- **[v3.2] 星表 = HYG Database + Stellarium星座線(DATA_SOURCES 優先3)**: 要件「300星+88星座」を達成。
  ラベル用288星(≤3.5等)+**線の頂点専用444星(等級カット外)**+88星座695線分。
  **等級カットの例外が本質**: 695線分のうち **520本(75%)が3.5等より暗い星を参照**しており、
  頂点用を落とすと画面上の線の8割が消える(実測)。`s`(ラベル用)と`v`(頂点専用)を分けて持ち、
  線の添字は `s.concat(v)` の連結空間。`make_stars.py` は参照HIPが全解決したか終了時に検査する。
  JSONの生配列だと23KBを超えたので、座標は `CORE.decodePoly`・等級と線添字は `CORE.decodeEle` で戻せる
  差分符号化にして **14.8KB**(目安15KB内)。新しいデコーダは足していない。
  クレジットは診断画面とREADMEに「星表: HYG Database (CC BY-SA) ／ 星座線: Stellarium」
- **[v3.2] 磁気偏角 = WMM自動算出(DATA_SOURCES 優先4)**: ハードコード7.5°をやめ、`--wmm WMM****COF.zip` で
  ルート重心・変換日の偏角を球面調和から算出する(次数12・永年変化込み・有効期間チェックつき)。`--dec` は上書き手段。
  **NOAA公式テスト値と一致**(12点/偏角の最大誤差 0.0046°/成分 0.05nT)。誰でも再現できるよう
  `--wmm-test` を用意した。日本の実算出値: 札幌9.92° 東京7.89° 那覇5.83°(2026.0)。
  つまり従来のハードコード7.5°は**札幌で2.4°ずれていた**
- **[v3.2 SPEC] B/A-1/C-1〜C-9/A-2/A-3 を実装**(`docs/SPEC_v3.2.md` の実装順1〜6)。
  A-2 の尾根線は抽出が不安定(高尾山実タイルで52px)なので `RIDGE_ON=false` で次段。
  A-3 は晴海の概形がOSMに道の無い岸壁を通っており、吸着では直らない(SPEC に実測を記載)。
  → 手描き概形そのものを廃止し、**晴海・皇居は OSM 道路網上の経路探索で生成**するようにした(下記 v3.2 道路網経路)。
  ↓は詳細ページ(勾配・次の登り・実効速度・引き返しマージン・テーマ)になった
- **[v3.2] 都市デモを道路網の経路探索で生成(`gpx2route.route_on_graph`)**: 手描き概形は皇居でも全アンカーが
  内堀通りの内側60〜110m(公園の小道)にあり、晴海は岸壁の上を通っていた。吸着(--snap-osm)は「道の近くのズレ」しか
  直せないので、概形を捨てて **交差点の角を数点だけ指定し、間は歩道優先の最短路で結ぶ** 方式にした。
  皇居 5.69km(桜田門→大手門→竹橋→半蔵門、歩道率>80%)、晴海フラッグ街区一周 1.65km。全10mサンプルが道路から2.2m以内。
  実装で効いた順: ①経由点をK本の辺への射影候補にして HMM(DP)で系列選択 ②遷移コストは重み付きコストでなく
  **実距離**で測る(歩道×0.9 だと直線より短く見えて車道中心の候補が勝ち、16mのひげが立った)
  ③車道系候補に歩道1本分(15m)の上乗せ ④「a→経由点→a」の往復を潰す ⑤周回は始点と終点を同じ辺に落とす
  ⑥角だけ指定するときは概形の回廊ペナルティを切る(角を結ぶ直線は本来の道から数百m離れる)。
  **コストモデル**は Valhalla pedestrian を土台にラン向けへ調整(`STREET_W`/`run_weight`): 幹線沿いの歩道・歩行者道 1.0、
  歩道タグ付き車道 1.1、住宅街路 1.3、歩道タグ無しの幹線 1.8、私道 1.6(駐車場通路 3.0)、未舗装 1.4、階段 5.0、
  `foot=no`/`access=private|no`/歩道無し trunk/高速は除外。横断歩道(`footway=crossing` の way)は1回ごとに
  距離換算 +15m、信号付きは +40m(信号待ちがランの最大の敵)。
  **皇居は角を置かず「皇居を囲む最短の閉路」で作る**(`enclosing_loop`: 中心から真東の半直線を跨ぐ辺を外し、
  跨ぐ辺ごとに両端を結ぶ最短路=一周)。角を門に置いた版は外苑側へ膨らんで 5.7km だったが、囲む閉路は
  内側の歩道リング **5.06km = 公式約5.0km**。CLI は `--ring-around LAT,LON`。`--route-osm` は概形を DP 40m で
  角だけに間引いて経由点にする(全点を経由点にすると路地でジグザグ)。
  `test/gpx2route.test.py [demo on OSM]` が routes.js の生成物を fixture の道路と突き合わせて守る(皇居 5.0±0.15km・
  反時計回り・横断≤20・全点が道路から5m以内)
- **[v3.2] その場モード(ルート無しのフリー走行)**: ルート選択の末尾「ここから」。実機で「その地点に行かないと
  始められない」のがストレスという要望から。現在地を起点に 1点だけのルートを作り、`along` を「動いた距離の積算」として
  扱うことで既存の表示(方位テープ・実効速度・目標ペース・地図の等高線・透視・星)がそのまま乗る。
  透視のレジストリはアプリ同梱の著名峰43+東京の目印4(150km/60km圏。全部「透視」=DEMレイキャスト無しなので破線)。
  偏角は WMM2025 を日本域で平面フィットした近似(`CORE.decJapan`、最大残差1.0°・主要都市±0.3°)。
  **出せないものは出さない**: 残距離(目標なし時)/CT/ETA/逸脱/引き返し/次の登り/断面/WP は非表示。再開対象にもしない。
  目標距離は なし/5km/10km(←→)、到達で帯に「到達」。目標ペースは段階2(未実装)。
  距離の積算は「最後に採用した良い測位」を基準にし、3m未満のジッタ・精度50m超・500m超の瞬間移動は足さない
- **[v3.2] 実機バグ修正**: tick が main/warn 以外で止まっていた(透視/星が顔に追従せず、記録も止まる)、
  方位イベントが矢印しか更新せずカクカクだった → 計測中は常に tick、方位イベントで表示を≈20fps描き直し
- **[v3.2] 検証チェーン⑥⑦の手順書化**: `docs/VERIFICATION.md`。画面は絵しか出さないので
  `__THUD.dumpGhost()` / `dumpSky()` で数字を出せるようにし、⑥は解析値(目標ペース=等速・標準CT=ctInverse)
  との一致、⑦はStellariumとの突き合わせ手順にした。⑦は**Stellarium無しでできる一次チェック**を先に置いた
  (天の北極の高度=観測地の緯度。実測で厳密一致。ここが通れば残る差は暦と歳差だけに絞れる)。
  この一次チェックは core.test.js に9件として機械化済み。歳差・大気差・固有運動の扱いは既知の系統差として明記
- **N9 ラップ・区間**: 周回検知+マッチャー巻き戻し、区間ボス+PB永続化、urbanドメインでの表示切替(残標高→ペース/Lap)
- 地形レイヤ・断面図・登頂儀式・自宅登録・診断拡張。天文計算(astro.js)は既知値と照合済み
- **[v3.1] 自宅登録の導線**: ready画面直下に常時案内(未登録なら高輝度「↓ ここを自宅にする」/登録済みなら削除導線)。診断パネル先頭にもピンチアクション行。二度押し確認は従来通り
- **[v3.1] ラップ進行方向ゲート**: 直近45秒の`along`履歴で「5秒以上かけて15m以上前進して起点圏内」のときのみ発火。うろうろ・後戻り・GPS復帰単発では発火しない。発火時に履歴クリア(`S.lapHist`)。`checkLapAndSegs` は `__THUD` に露出済みでテストから直接叩ける
- **[v3.1] gpx2route.py v3拡張**: 任意GPXにOSM由来のPOI(温泉/駅/小屋/神社。shop/towerはオプトイン)と山座レジストリを焼き込める。オフライン2段構え:
  `--emit-query` でOverpass QLを生成 → ユーザーが手元で実行しJSON保存 → `--osm poi.json` で投入。
  内蔵著名峰DB(全国43峰)を150km圏でマージ(OSM同名はOSM座標優先)。POIはルート1.5km圏・峰は重心30km圏でローカルフィルタ。
  `--seg "0.08-0.22:名前"`(割合)/`"1200-1800:名前"`(実距離m)、`--vis`(実線の手動確証)、`--dec`、`--domain auto`(獲得15m/km閾値)、`--dump-json`(検証用)。
  エンドツーエンド確認済み: 出力は `CORE.buildRoute` でそのまま読める

### ✗ 未達(優先度順)

1. **【技術的に不可能・仕様側で解決済み】夜間の最低輝度強制**
   Web APIから輝度制御は不可。案内文+暗赤パレットで代替済み。体験設計書側も修正済み

**それ以外の残タスクは v3.2 で全て消化した。** 次にやるなら「5. 推奨する次の一手」を見る。

### 既知の不整合

(v3.2 で解消) 断面パネルとサマリーの「ゴースト比」は全ソースで実ゴースト基準(`CORE.ghostDelta`)になった。

---

## 2. ファイル構成

```
CLAUDE.md              Claude Code向け運用指示(制約・コマンド・思想)
docs/
├── HANDOFF.md         この文書
├── DATA_SOURCES.md    データソース改定仕様(優先1〜4。v3.2で全完了)
├── VERIFICATION.md    検証チェーン⑥⑦の手順書(既知ゴースト照合・Stellarium照合)
└── meta-display-dev-guide.md  プラットフォーム開発ガイド(5原則・配色・入力・デプロイ)
src/
├── core.js       純ロジック層(距離計算・逸脱FSM・周回回転など) — node直require可
├── app.js         アプリ層(画面・状態・入力処理。最大のファイル)
├── astro.js       天文計算(太陽・月・惑星・恒星の方位仰角)
├── routes.js      デモ4ルートのデータ(自動生成・手編集しない)
├── stars.js       星表データ(自動生成・手編集しない)
├── style.css
└── template.html  ビルドの型
tools/
├── build.py           単一HTML組み立て(BUILD/ROUTES/STARS/ASTRO/CSS/CORE/APPスロット)
├── make_field_demo.py デモ4ルート生成→routes.js(晴海・皇居=OSM道路網の経路探索+vec焼き込み / 高尾・富士=概形)
├── make_registry.py   デモ4ルートのレジストリ焼き込み→routes.js(手書き精選DB。実運用はgpx2route.py)
├── make_stars.py      星表生成→stars.js
└── gpx2route.py       実GPX→v3ルート変換(v3.2でDEM投入口。配布物。stdlibのみ)
test/
├── core.test.js       純ロジック 156件
├── app.smoke.js       jsdom統合 149件(sim経由の全画面フロー+v3.1回帰: 自宅導線・ラップゲート+v3.2: 目標ペース)
└── gpx2route.test.py  変換ツール 94件(OSM分類・seg・domain+v3.2 DEM: デコード/fetch/レイキャスト/ele検査、道路ベクタ: 分類/吸着/予算)
dist/
├── index.html     ビルド成果物(これを配布・デプロイする)
└── sw.js           Service Worker v3(地理院タイルキャッシュ対応)
```

**ビルド手順**: ソースを編集したら `python3 tools/build.py`。デモルートデータを触ったら `make_field_demo.py` → `make_registry.py` → `make_stars.py` の順で再生成してから build。

---

## 3. デプロイ

GitHub Pages(リポジトリ `tozan-hud`、Public必須)。`dist/index.html` と `dist/sw.js` を差し替えて push。
v3でsw.jsが変わっているので、v2以前からの継続なら両方差し替え。反映は1〜10分、ルート選択画面右下のbuild IDで版を確認。

---

## 4. プラットフォーム既知事実(引き継ぎ必須・実機で確定済み)

**該当コードを触る前に必ず読む。** 体験設計書§10と同内容。

1. センサー権限は「履歴変更前」かつ「同一ジェスチャー内で同期発行」でないと`requestPermission`が永久pending
2. グラスの方位は`alpha`直がコンパス方位(iOSの`webkitCompassHeading`とは別・標準Androidの`360-α`とも別)。診断画面で`hmode`切替可能
3. 方位は起動後数秒デタラメ→円形EMA平滑+較正ゲート実装済み(`headingSettled`)
4. `watchPosition`は静止時に報告停止→20秒無報告でキープアライブ単発測位を実装済み
5. Notifications非対応→プル型設計(`visibilitychange`/`pageshow`)
6. バックはシェルが`history.back()`、pushState上限5段
7. オフライン起動にはSW必須、キャッシュファーストは更新不能になる→ネット優先+4秒フォールバック(sw.js実装済み)
8. **【要実機確認・1回だけ】仰角(pitch)の符号と原点。** 空レイヤは `beta` に追従する(SPEC B-5)。
   端末を立てた姿勢が `beta≈90` という前提で `pitch = beta - 90`(水平=0・見上げ=+)としている。
   確認方法: 診断画面(ready で↓)に `β` と `→ ピッチ` を並べて出してある。**水平を向いてピッチが0付近なら正**。
   ずれていたら `src/app.js` の `oriHandler` 内の1行(`e.beta - 90`)を直す。方位の `hmode` と同じ種類の未知数

---

## 5. 推奨する次の一手

§1の残タスクは実質ゼロ。次は**実GPXでの実戦投入**が良い。gpx2route.py v3は合成データで検証済みだが、実物のOverpass応答は想定外のタグ構成を含み得る。最初の1本は `--dump-json` で中身を確認しながら変換し、問題があれば `OSM_CLASSIFY` の分類規則に足す。
実GPXが1本通ったら、実機テスト(視認性・方位の体感・ジェスチャーの3点)へ進む。

---

## 6. テストの状態

- `node test/core.test.js` → 156/156(v3.2でDEM 18件、地図ベクタ7件、星表10件、天球座標変換9件を追加)
- `node test/app.smoke.js` → 149/149(v3.0の45件+v3.1回帰11件: 自宅登録導線のUI文言/二度押し/永続化、ラップゲートのうろうろ・後戻り・前進通過・巻き戻し+v3.2の14件: 目標ペースの層開閉・5分きざみ・下限・キャンセル・永続化・等速位置・端クランプ・lap1に奪われない)
- `python3 test/gpx2route.test.py` → 94/94(v3.2でDEM 29件+道路ベクタ/吸着18件+WMM 17件を追加)
- 実機でしか分からないのは「視認性・方位の体感・ジェスチャー」の3点のみ、という原則は維持
